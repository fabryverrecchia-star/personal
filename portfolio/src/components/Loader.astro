---
import { site } from '../site.config';
---

<div class="loader" data-loader transition:persist="loader" aria-hidden="true">
  <div class="loader__inner container">
    <span class="loader__name name">{site.name}</span>
    <span class="loader__count figures"><span data-loader-count>0</span></span>
  </div>
</div>

<style>
  .loader {
    display: none;
  }
  @media (scripting: enabled) {
    .loader {
      display: block;
      position: fixed;
      inset: 0;
      z-index: 100;
      background: var(--bg);
    }
  }
  .loader.is-done {
    display: none;
  }
  .loader__inner {
    position: absolute;
    inset: auto 0 0 0;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    padding-bottom: var(--margin);
  }
  .loader__name {
    font-size: var(--fs-xl);
    line-height: 1;
    letter-spacing: -0.035em;
  }
  .loader__count {
    font-size: var(--fs-xxl);
    line-height: 0.75;
  }
</style>
