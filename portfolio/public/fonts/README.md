# Polices

Fichiers attendus (déjà branchés dans `src/styles/global.css`) :

- `EBGaramond-Italic-VariableFont_wght.ttf`, et idéalement sa version `.woff2` (même nom)
- La police sans-serif de la marque : la déposer ici, puis mettre à jour le bloc `@font-face "Brand Sans"`

Une fois les fichiers reçus : conversion en `.woff2` et réduction aux caractères utilisés (latin), pour un poids minimal.
