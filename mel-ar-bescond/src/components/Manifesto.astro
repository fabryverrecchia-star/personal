<section class="manifesto" id="manifeste">
  <div class="wrap manifesto__grid">
    <p class="label manifesto__kicker" data-reveal>Notre promesse</p>
    <div class="manifesto__body">
      <p class="manifesto__text" data-words>
        Au cœur du bocage breton, à Louargat, nos abeilles butinent à leur rythme. Nous ne récoltons que ce que la ruche
        veut bien offrir&nbsp;: un miel <em>rare</em>, patient, fidèle à sa saison.
      </p>
      <dl class="manifesto__def" data-reveal>
        <div>
          <dt><em>Mel</em></dt>
          <dd class="label">Le miel, en breton</dd>
        </div>
        <div>
          <dt><em>ar Bescond</em></dt>
          <dd class="label">De Coat ar Bescond, notre lieu-dit</dd>
        </div>
      </dl>
    </div>
  </div>
</section>

<style>
  .manifesto {
    padding-block: clamp(120px, 18vw, 280px);
  }
  .manifesto__grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 3fr);
    gap: 2rem;
  }
  .manifesto__kicker {
    color: var(--ink-soft);
    padding-top: 1.1em;
  }
  .manifesto__text {
    font-size: clamp(2rem, 4.4vw, 4.8rem);
    line-height: 1.12;
    font-weight: 300;
    letter-spacing: -0.015em;
    text-wrap: pretty;
  }
  .manifesto__text em {
    font-style: italic;
    color: var(--gold);
  }
  .manifesto__text :global(.word) {
    display: inline-block;
  }
  .manifesto__def {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 2rem;
    margin-top: clamp(64px, 8vw, 120px);
    max-width: 760px;
  }
  .manifesto__def div {
    display: grid;
    gap: 0.6rem;
    padding-top: 1.2rem;
    border-top: 1px solid var(--line);
  }
  .manifesto__def dt {
    font-size: clamp(1.8rem, 2.6vw, 2.6rem);
    font-weight: 300;
    line-height: 1;
  }
  .manifesto__def dd {
    color: var(--ink-soft);
  }

  @media (max-width: 800px) {
    .manifesto__grid {
      grid-template-columns: 1fr;
    }
    .manifesto__def {
      grid-template-columns: 1fr;
      gap: 1.2rem;
    }
  }
</style>
